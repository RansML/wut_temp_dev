language: python

cache: bundler

python:
  - "3.6" 
  
before_install:
  - pip install -U pip
  - python setup.py install
  
install:
  - pip install -r requirements.txt
  - pip install -e .

script:
  - pytest
